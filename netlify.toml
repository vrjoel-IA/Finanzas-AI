[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# El escaneo de secretos vuelve a estar activo: si alguna vez una clave acaba
# otra vez dentro de dist/, el despliegue falla en lugar de publicarla.
